// Crie uma função que receba dois argumentos e retorne a soma dos mesmos
let soma = (numero01, numero02) =>  numero01 + numero02;
soma(2,2);

// Declare uma variável que receba a invocação da função criada acima, passando dois números quaisquer por argumento, e somando 5 ao resultado retornado da função.
let soma02 = (numero01, numero02) => {
  return numero01 + numero02;
}
soma02(2,3);

// Qual o valor atualizado dessa variável?
soma02(2,3);

// Declare uma nova variável, sem valor
semValor = ''

// Crie uma função que adicione um valor à variável criada acima, e retorne a string: O valor da variável agora é VALOR. Onde VALOR é o novo valor da variável;
let addValor = (semValor) => { semValor = 10
  return semValor
}